import React from 'react';
import './AppDescription.sass';

const AppDescription = () => (
    <div className="b-app-description">
        <h2 className="b-app-description__head">Application description</h2>
    </div>
);

export default AppDescription;